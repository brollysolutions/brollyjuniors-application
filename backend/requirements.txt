# --- web -------------------------------------------------------------------
fastapi==0.121.2
uvicorn[standard]==0.41.0
python-multipart==0.0.20

# --- data ------------------------------------------------------------------
# asyncpg speaks Postgres' native $1 placeholders, which is why every SQL
# statement carried over from the TypeScript build runs unchanged.
asyncpg==0.31.0
redis==6.4.0

# --- auth ------------------------------------------------------------------
PyJWT==2.10.1
cryptography==46.0.3

# --- config ----------------------------------------------------------------
pydantic==2.12.4
pydantic-settings==2.12.0
python-dotenv==1.2.1
